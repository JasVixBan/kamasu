#ifndef RESOPHONIC_UNARYFUNCTIONEXPRESSIONS_HPP_INCLUDED
#define RESOPHONIC_UNARYFUNCTIONEXPRESSIONS_HPP_INCLUDED

namespace resophonic 
{
  namespace kamasu 
  {
    /*'\n'.join(['Expression<boost::proto::terminal<tag::%s>::type> const %s = {{}};' % (fn, fn) for fn in functions])*/
    
  }
}

#endif
