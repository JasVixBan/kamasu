#ifndef RESOPHONIC_UNARYFUNCTIONS_HPP_INCLUDED
#define RESOPHONIC_UNARYFUNCTIONS_HPP_INCLUDED

namespace resophonic 
{
  namespace kamasu 
  {
    /*'\n'.join(['''
      template <class Arg>
      typename boost::proto::result_of::make_expr<%s_tag, Domain, Arg const &>::type
      %s (Arg const& arg) {
        return boost::proto::make_expr<%s_tag, Domain>(boost::ref(arg));
      }
      ''' % (fn, fn, fn) for fn in functions])*/
  }
}

#endif
